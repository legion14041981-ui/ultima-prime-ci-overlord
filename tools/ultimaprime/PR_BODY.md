# 🔧 ULTIMA-PRIME: Предложенные исправления CI

## Краткое описание

Этот PR содержит автоматически сгенерированные патчи для исправления критических ошибок CI и QA:

- ✅ Добавлены недостающие импорты (`Optional` из `typing`)
- ✅ Добавлены отсутствующие зависимости (напр. `RestrictedPython`)
- ✅ Предложены изменения для миграции на Pydantic v2 (где необходимо)
- ✅ Исправлены неизвестные pytest-маркеры

## Диагностический отчёт

Подробный отчёт доступен в:
- `diagnostics/report.json` (машиночитаемый)
- `diagnostics/report.txt` (человекочитаемый)

## Что проверить локально

### 1. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 2. Запуск тестов
```bash
pytest -v
# или с покрытием
pytest -v --cov=src
```

### 3. Проверка стиля
```bash
ruff check src/
```

### 4. Security scan
```bash
bandit -r src/
```

### 5. Проверка типов (если используется)
```bash
mypy src/ --strict
```

## Чек-лист перед merge

- [ ] Локальные тесты прошли (`pytest -q` зелёный)
- [ ] CI checks прошли (зелёные галочки)
- [ ] Code review завершён
- [ ] Backup/rollback план готов
- [ ] Changelog обновлён (если требуется)
- [ ] Ответственный (Release Committee) одобрил

## 🚨 Политика безопасности

**ЭТО DRAFT PR.** Требуется ручное подтверждение перед merge.

### Принципы ULTIMA-PRIME:
1. **Dry-run сначала** — все изменения просмотрены
2. **Review обязателен** — никаких автоматических слияний
3. **Полная прозрачность** — все изменения задокументированы
4. **Откатываемо** — план восстановления готов

## Контакты

- 👤 **Генератор**: ULTIMA-PRIME.CI-OVERLORD (v1.0.0)
- 📅 **Дата**: <!-- generated_date -->
- 🔗 **Диагностика**: See artifacts in workflow run

---

**ВАЖНО:** Не мержить без явного одобрения! Обратись к Release Committee.
